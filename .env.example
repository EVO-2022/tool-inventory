# NocoDB Public URL (optional; leave blank for local development)
# For production, set this to your public URL, e.g., https://tools.yourdomain.com
NC_PUBLIC_URL=

# NocoDB JWT Secret (optional; NocoDB will generate one if not provided)
# For production, set a strong random string
NC_AUTH_JWT_SECRET=
